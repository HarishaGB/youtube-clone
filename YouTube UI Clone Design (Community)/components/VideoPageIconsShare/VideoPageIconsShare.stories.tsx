import {VideoPageIconsShare as Component} from './VideoPageIconsShare';
import type {StoryObj, Meta} from '@storybook/react';

type Story = StoryObj<typeof Component>;

const meta: Meta<typeof Component> = {
  title: 'Video-Page/Icons/Share',
  component: Component,
};

export const VideoPageIconsShare: Story = {
  // ...
};

export default meta;
