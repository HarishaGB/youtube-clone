import {VideoPageIconsSave as Component} from './VideoPageIconsSave';
import type {StoryObj, Meta} from '@storybook/react';

type Story = StoryObj<typeof Component>;

const meta: Meta<typeof Component> = {
  title: 'Video-Page/Icons/Save',
  component: Component,
};

export const VideoPageIconsSave: Story = {
  // ...
};

export default meta;
