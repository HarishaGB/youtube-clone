import {VideoPageIconsLiked as Component} from './VideoPageIconsLiked';
import type {StoryObj, Meta} from '@storybook/react';

type Story = StoryObj<typeof Component>;

const meta: Meta<typeof Component> = {
  title: 'Video-Page/Icons/Liked',
  component: Component,
};

export const VideoPageIconsLiked: Story = {
  // ...
};

export default meta;
