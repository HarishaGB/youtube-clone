import {VideoPageIconsDisLiked as Component} from './VideoPageIconsDisLiked';
import type {StoryObj, Meta} from '@storybook/react';

type Story = StoryObj<typeof Component>;

const meta: Meta<typeof Component> = {
  title: 'Video-Page/Icons/DisLiked',
  component: Component,
};

export const VideoPageIconsDisLiked: Story = {
  // ...
};

export default meta;
