import {SidebarIconsPlay as Component} from './SidebarIconsPlay';
import type {StoryObj, Meta} from '@storybook/react';

type Story = StoryObj<typeof Component>;

const meta: Meta<typeof Component> = {
  title: 'Sidebar-Icons/play',
  component: Component,
};

export const SidebarIconsPlay: Story = {
  // ...
};

export default meta;
