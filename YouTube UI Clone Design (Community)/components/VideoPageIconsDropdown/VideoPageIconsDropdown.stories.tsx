import {VideoPageIconsDropdown as Component} from './VideoPageIconsDropdown';
import type {StoryObj, Meta} from '@storybook/react';

type Story = StoryObj<typeof Component>;

const meta: Meta<typeof Component> = {
  title: 'Video-Page/Icons/Dropdown',
  component: Component,
};

export const VideoPageIconsDropdown: Story = {
  // ...
};

export default meta;
